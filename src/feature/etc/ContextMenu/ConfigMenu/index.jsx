import { Mouse } from '@material-ui/icons';

import View from './View';
import Info from '../FeatureInfo';

export default {
  key: Info.ID,
  group: 'etc',
  Icon: Mouse,
  label: Info.name,
  View,
};
